export type Product = {
    id: string
    name: string
    price: number
    description?: string
    image?: string
    owner?: string
    category?: string
    brand?: string
    currency?: string
}
